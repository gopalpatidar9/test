
<h2 class="artical-list"><%=  @artical.title.capitalize %></h2>

<div class="row justify-content-md-center">
<div class="col-8">
<div class="container-md">
  <div class="card text-center" id="shedo">
    <div class="card-header bg-info text-white">
      <em>By Gopal Patidar</em>
    </div>
    <div class="card-body">
      <div class="card-text text-left"><%= simple_format(@artical.discription.capitalize)%></div>
      <p><%= link_to"Edit" , edit_artical_path(@artical) , class:"btn btn-outline-info"%>
      <%= button_to"Delete" , artical_path(@artical) , method: :delete ,  data:  {turbo_confirm: "Are you sure?"} , class:"btn btn-outline-danger"%>
    </div>
    <div class="card-footer text-muted">
      <small> Created <%= time_ago_in_words(@artical.created_at)%> ago </small>
    </div>
  </div>
</div>  
<%= link_to "Backe to Articals" , articals_path , class: "btn btn-primary"%></a></p>
